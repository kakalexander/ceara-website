"use client";

/**
 * @deprecated Substituído por `RevealInit`. Mantido como stub vazio
 * para não quebrar imports antigos que ainda referenciem ScrollReveal.
 */
export function ScrollReveal(): null {
  return null;
}
